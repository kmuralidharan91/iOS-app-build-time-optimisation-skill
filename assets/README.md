# Assets

Banner, screenshots, and demo recordings for the project README and
docs. Add files here in Phase B (alongside the public-citation
backfill); reference them from the project root README.

Pending:

- `banner.png` (or `.svg`) — repo-card banner for the README header.
- `demo-doctor-loop.gif` — short before/after capture of
  `ios-build-doctor` running on a public iOS project (Wikipedia-iOS,
  NetNewsWire, or Telegram-iOS).
- `screenshot-diagnose-output.png` — typical `ios-build-diagnose`
  rendered output.

Until those land, the README's "Sample run" section uses a verbatim
text transcript instead of an image reference.
